<h1>EAT-DA-BURGER</h1>
<h3>Choose a burger and DEVOUR it</h3>

<div class="row">


</div>


<div class="row">
    <h2 class="col-md-6">Available Burgers</h2>
    <ul class="col-md-6">

        {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=true}} {{/unless}} {{/each}}
    </ul>

    <h2 class="col-md-6">Devoured Burgers</h2>
    <ul class="col-md-6">
        {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=false}} {{/if}} {{/each}}
    </ul>

</div>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="burger_name" placeholder="Enter Burger">
        <input type="text" id="de" name="short_description" placeholder="Enter Description">
    </div>

    <div class="form-group">
        <label for="slee">Devoured?</label>
        <br>
        <input type="radio" name="devoured" value="1" checked> Devoured Immediately
        <br>
        <input type="radio" name="devoured" value="0"> Not Devoured
    </div>

    <button type="submit">Add Da Burger</button>
</form>